import createPlaceholderImage from '../utils/create-placeholder.js';

// Создаем изображение-заполнитель при запуске скрипта
const placeholderPath = createPlaceholderImage();
console.log(`Placeholder image created at: ${placeholderPath}`); 