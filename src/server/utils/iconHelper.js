/**
 * Утилита для генерации иконок категорий экспонатов
 */

/**
 * Генерирует эмодзи-иконку на основе категории экспоната
 * @param {string} category - Название категории
 * @returns {string} - Эмодзи-иконка, соответствующая категории
 */
export const generateCategoryIcon = (category) => {
  if (!category) return '🔌';
  
  const categoryLower = category.toLowerCase();
  
  // Карта соответствия категорий и иконок
  const iconMap = {
    'компьютеры': '💻',
    'компьютер': '💻',
    'телефоны': '📱',
    'телефон': '📱',
    'аудио': '🎧',
    'видео': '📹',
    'игры': '🎮',
    'игровые устройства': '🎮',
    'калькуляторы': '🧮',
    'телевизоры': '📺',
    'фотоаппараты': '📷',
    'фотоаппарат': '📷',
    'часы': '⌚',
    'музыкальные плееры': '🎵',
    'плееры': '🎵'
  };
  
  // Поиск иконки по категории
  for (const [key, icon] of Object.entries(iconMap)) {
    if (categoryLower.includes(key)) {
      return icon;
    }
  }
  
  // Иконка по умолчанию
  return '🔌';
}; 